# Copy this file to .env and fill in your real values.
# docker-compose reads these variables when starting the stack.

# --- Database (used by the MySQL db container + app) ---
MYSQL_ROOT_PASSWORD=rootpass
DB_USER=monica
DB_PASS=monica123
DB_NAME=st_monica

# --- Auth ---
# Use a long random string in production
JWT_SECRET=change-this-to-a-long-random-secret

# --- SMTP Email (Gmail app password) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=stmonicayouthongatarongai@gmail.com
SMTP_PASS=your-16-character-gmail-app-password
SMTP_FROM=stmonicayouthongatarongai@gmail.com
MAIL_FROM_NAME=St.Monica Youth Ongata Rongai
